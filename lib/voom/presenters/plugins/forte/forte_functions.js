document.addEventListener('DOMContentLoaded', () => {
    document.getElementById('forte-echeck-form').addEventListener('submit', (submitEvent) => {
        submitEvent.preventDefault();

    });
});
